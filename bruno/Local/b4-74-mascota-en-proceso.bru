meta {
  name: B4 - 74 - Mascota EN_PROCESO
  type: http
  seq: 74
}

get {
  url: {{baseUrl}}/mascotas/{{mascotaEnProcesoId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("no publica una mascota en proceso", function() {
    expect(res.status).to.equal(404);
    expect(res.body.message).to.equal("No se encontró la mascota solicitada");
  });
}
