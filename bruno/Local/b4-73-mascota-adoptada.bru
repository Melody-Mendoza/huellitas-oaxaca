meta {
  name: B4 - 73 - Mascota ADOPTADO
  type: http
  seq: 73
}

get {
  url: {{baseUrl}}/mascotas/{{mascotaAdoptadaId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("no publica una mascota adoptada", function() {
    expect(res.status).to.equal(404);
    expect(res.body.message).to.equal("No se encontró la mascota solicitada");
  });
}
