meta {
  name: B4 - 72 - ID inexistente
  type: http
  seq: 72
}

get {
  url: {{baseUrl}}/mascotas/{{mascotaInexistenteId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("oculta recursos inexistentes", function() {
    expect(res.status).to.equal(404);
    expect(res.body.message).to.equal("No se encontró la mascota solicitada");
  });
}
